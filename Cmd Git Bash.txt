----First
$ git add .

$ git commit -a -m "commit to repository"

$ git remote add BIGSCHOOL https://github.com/luckythang1133/BigSchool.git

$ git push --set-upstream BIGSCHOOL master

$ git clone https://github.com/luckythang1133/BigSchool.git

----Add from clone
//add + commit
$ git commit -a -m "commit to repository" 

$ git push